import { space } from './space';

// IMPORTANT: Numbers don't work here, use strings.
export const sizes = {
    ...space,
    container: {
        maxWidth: '1196px',
    },
    header: {
        maxWidth: '1440px',
    },
};
